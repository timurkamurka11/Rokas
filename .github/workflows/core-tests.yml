name: Core behavior
on:
  push:
    branches: [development]
  pull_request:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  core:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - name: Literal git diff check
        run: git diff --check
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'
      - name: Compile and exercise the real C# domain
        run: dotnet run --project Tests/Core/Rokas.Core.Tests.csproj --configuration Release
      - name: Validate artwork, audio, metas and scene bindings
        run: python3 Tools/validate_assets.py
